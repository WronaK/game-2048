export enum KeyCode {
    LEFT = "ArrowLeft",
    UP = "ArrowUp",
    RIGHT = "ArrowRight",
    DOWN = "ArrowDown",
}

export enum TypeTile {
    EMPTY,
    NEW,
    ORDINARY,
    CONNECTED,
}

export enum Direction {
    RIGHT,
    LEFT,
    DOWN,
    UP,
}